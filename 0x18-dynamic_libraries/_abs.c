#include "main.h"

/**
 * _abs - check if @n is absolute value of an integer
 * @n: integer to check absolute value of
 *
 * Return: The absolute value of n.
 */
int _abs(int n)
{
	return (n >= 0 ? n : -n);
}

